# 设置控制台日志编码
spring.output.ansi.enabled=always
logging.charset.console=UTF-8
logging.pattern.console=%clr(%d{${LOG_DATEFORMAT_PATTERN:yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:%wEx}

# 多吉云图床配置
app.image.access-key=84c60ca748804658
app.image.secret-key=366077a9842fc7962eb966de1b6c6aa9
app.image.bucket=user-img-data
# 多吉云OSS上传接口地址
app.image.upload-url=https://api.dogecloud.com/oss/upload
# 允许的文件类型
app.image.allowed-types=image/jpeg,image/png,image/gif,image/webp
# 最大文件大小（字节），此处为5MB
app.image.max-file-size=5242880

# 图片缩略图配置
app.image.thumbnail.width=200
app.image.thumbnail.height=200

# 图片上传限制
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 自定义图片域名
app.image.custom-domain=https://usr.dannysummer.asia